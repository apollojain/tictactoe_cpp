#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include "Board.h"

#include "doctest.h"

#include <unistd.h>

using namespace std;

SCENARIO("simple game check") {

	Board b1;
	// test 1: place at a spot that is 
	// int p1 = b1.currentPlayer;
	// CHECK(b1.isValidPlacement(0, 0) == true);
	// CHECK(b1.place(0, 0) == true);
	// CHECK(p1 == 1);
  
} 